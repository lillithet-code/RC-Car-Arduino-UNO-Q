# Unified installer defaults
# Loaded automatically by install_unified.sh

# Git update settings
REPO_REMOTE=origin
REPO_BRANCH=main

# Deployment targets
PLESK_DOMAIN=drive.kbob.org
MEDIAMTX_WHEP_BASE=https://drive.kbob.org
MEDIAMTX_RTSP_BASE=rtsp://drive.kbob.org:8554
MEDIAMTX_WEBRTC_ADDITIONAL_HOST=drive.kbob.org
SERVER_URL=https://drive.kbob.org
BOARD_NAME=RCCar1

# Known repository locations from current setup
SERVER_APP_DIR=/var/www/vhosts/drive.kbob.org/httpdocs/rc-car-arduino-uno-q
BOARD_DIR=/home/arduino/rc-car-arduino-uno-q

# Optional: set BOARD_TOKEN here once if auto-discovery cannot find it
# BOARD_TOKEN=replace-with-real-token
